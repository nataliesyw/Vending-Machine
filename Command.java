package asm3;

/**
 * the class command is used to perform different actions on the vending machine
 *
 */
public class Command{
	/**
	 * the execute method that performs corresponding actions and return the result
	 * @param v the VendingMachine object that is passed into the method
	 * @param cmdPart the String of the command content
	 * @return the command execution result
	 */
	public String execute(VendingMachine v, String cmdPart){
		String result = null;
		return result;
	}
}
